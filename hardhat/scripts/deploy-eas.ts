// script to deploy EAS contracts to local hardhat network
import { ethers } from "hardhat";

async function main() {
  console.log("Deploying EAS contracts...");
  // ... actual deployment logic ...
}

main().catch((error) => {
  console.error(error);
  process.exitCode = 1;
});
