// ─── On-Chain Resolver Public E2E ───────────────────────────────────────────

import { describe, it, expect } from 'vitest';

describe('onchain-resolver-public', () => {
  it.todo('should return 200 with all fields for a confirmed mirror');
  it.todo('should show revocation status for a revoked attestation');
  it.todo('should return 503 when Base RPC is unreachable');
  it.todo('should rate-limit at RPM threshold');
});
