import { describe, it } from 'vitest';

describe('onchain-mirror-happy-path', () => {
  it.todo('should seed VC + opt-in + request mirror');
  it.todo('should process via dispatcher on local Hardhat');
  it.todo('should verify attestation on chain');
  it.todo('should resolve publicly via resolver page');
  it.todo('should include reputation bonus for score >= 90');
});
