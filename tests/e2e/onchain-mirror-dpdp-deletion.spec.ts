import { describe, it } from 'vitest';

describe('onchain-mirror-dpdp-deletion', () => {
  it.todo('should enqueue bulk unmirror on student deletion');
  it.todo('should revoke all EAS attestations via unmirror dispatcher');
  it.todo('should create revocation records for each attestation');
});
